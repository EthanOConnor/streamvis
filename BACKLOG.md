# Backlog

- Port to Apptron and publish as a GitHub Pages site.

- Refine latency-aware polling and control strategy:
  - Use full per-update latency windows (lower/upper bounds) to tune polling windows.
  - Keep overall call volume low while converging on second-level accuracy for stable stations.
